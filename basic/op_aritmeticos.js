const [a, b, c, d] = [3, 4, 1, 15];

const soma = a + b + c + d;
const subtacao = b - c;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

console.log(soma, subtacao, multiplicacao, -divisao, modulo);
